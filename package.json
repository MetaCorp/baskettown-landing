{
  "name": "baskettown-landing-front",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.0.1",
    "@material-ui/icons": "^4.0.1",
    "classnames": "^2.2.6",
    "jsonp": "^0.2.1",
    "keyboardjs": "^2.5.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-on-visible": "^1.6.0",
    "react-pose": "^4.0.8",
    "react-scripts": "3.0.1",
    "react-use": "^9.1.2",
    "rebound": "^0.1.0",
    "to-querystring": "^1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "generate:svg-components": "svgr --ext jsx --icon --expand-props end --out-dir ./src/assets/generated ./src/assets/images",
    "now-build": "yarn test && react-scripts build",
    "now-dev": "yarn start"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@svgr/cli": "^4.3.0"
  }
}
